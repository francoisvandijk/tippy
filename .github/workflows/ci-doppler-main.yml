name: CI (Doppler Main)

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Install Doppler CLI
        run: curl -sLf https://cli.doppler.com/install.sh | sh

      - name: Login to Doppler
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN_CI }}
        run: doppler login --token "$DOPPLER_TOKEN"

      - name: Run tests with Doppler env (development)
        run: |
          doppler run --doppler-config=development -- \
          bash -lc 'node scripts/print-env-check.js; echo ok'
