# PR Creation Command

After installing and authenticating with GitHub CLI, run:

```powershell
gh pr create --base main --head infra/doppler-setup --title "Doppler Secrets Management Setup — Draft (§25)" --body "Adds Doppler workflow, runbook, rotation policy, and env verification. No secrets included. See ops/doppler/README.md." --draft
```

